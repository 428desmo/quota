# Quota Web 実装仕様

| 項目 | 内容 |
|---|---|
| 版 | 1.2 |
| 対応する規則 | quota_rule_v1.5.md |
| 対象 | `python -m quota.web` |

規則の正本はルール文書である。本書はブラウザ版の画面と通信だけを定める。盤面はサーバが1つ持ち、開いているブラウザは同じゲームを見る。

## 1. 起動

```bash
python -m quota.web
```

`0.0.0.0:8000` で待ち受ける。同じネットワークの端末は、起動したマシンのアドレスに `:8000` を付けて開く。

## 2. 画面

1. スタート画面。アイテムセット、人数（3か4）、人間の席の数、任意のシード、上級ルールの有無を選ぶ。選択肢は `quota_item_sets_v1.0.json`。既定は交易品。「スタート」でゲームが始まる。
2. ゲーム画面。場札と各席のノルマ、収集札、実績を出す。用語は規則の用語を使う。カードの名前・絵文字・数字ラベル・色は、そのゲームのアイテムセットに従う。手番の席が人間なら、そのブラウザから行動できる。CPU の手番はサーバが進める。各席の枠は、ゲーム開始時から収集の1行と実績2行分の高さを空けておく。札が増えても席の枠は縦に伸びない。
3. 終了時は結果を重ねて出す。「OK」でスタート画面に戻る。
4. 「途中でやめて最初からやり直す」は、ゲーム中いつでもスタート画面に戻す。全員の画面が戻る。ゲーム終了後の結果表示中は、このボタンを出さない。

席どうしは枠で区切らない。背景色の違いで席の境界を示し、手番の席だけ背景を変える。スマホの縦画面（幅 700px 以下）では、札の幅と高さをそれぞれ通常の 60% にし、場札・ノルマ・実績の見出しは左に縦書きにして行を消費しない。放棄・パスは説明文の左に並べ、説明文はその右で折り返す。

見た目は枠線と絵文字、文字だけとする。カード id は出さない。数字欄はアイテムセットの `rank_labels`、ワイルドは `wild_rank_label`（交易品では 1〜13 と `＊`）を書く。縁は実線のまま、数字の色だけセットの色を使う。

実績では、各達成の束の先頭（ノルマ札）の数字の下に、数字と重ならない位置で、達成ボーナスの点数だけ小さい 🟡 を縦に、印どうしも重ならない間隔で並べる。上級ルールのとき、隣り合う札の並び順ボーナスは、つながりの後ろの札の同じ位置に、その組の点数だけ小さい 🟣 を縦に並べる。

ノルマ札と収集札は同じ列に置く。収集札どうしは幅の約3分の1を重ね、ノルマ札と収集札の左端は幅の約4分の1を重ねる。数字は札の左上に置き、重なっても読めるようにする。実績は、隣り合う札が幅の約2分の1重なる。

終了時の得点の枠は、動いている札より手前に出す。

交易品セットの対応は、香辛料 🌶️、絹 🎀、茶 🫖、宝石 💎、銀貨 🪙。他のセットは JSON の絵文字と名前を使う。

「膠着済み」は、膠着で場を入れ替えたあと、まだ誰も取っていない状態である。

## 3. 操作

- ノルマがないとき、場札の通常カードを押すとノルマ札にする。銀貨は押せない。パスはボタン。
- ノルマがあるとき、取れるカードを1枚押すとその場で集める。手番は続く。押せないカードは薄くする。「集める」ボタンはない。続けて別の有効札も取れる。それ以上取らないとき、または取れる札が残っていないときは「パス」で次の席に回す。ノルマが揃ったときは、パスなしで次の席に回る。
- 「放棄」と「パス」はボタン。

## 4. アニメーション

カードを取ったとき、場札に出ているその札そのものを、置かれる位置まで動かす。移動が終わるまで、ノルマ・収集・実績の側には同じ札を出さない。着いてから、そこが収集札または実績になる。別の札を場札の上に作って飛ばすことはしない。

1 のノルマ札は、場札から一度収集の列に置き、約 0.1 秒たってから実績へ移す。集めてノルマが揃ったときも、揃った束をいったん収集の列に見せ、一呼吸（約 0.4 秒）置いてから実績へ移す。実績へ直接は置かない。

実績は、1行に収まらない長さになったら次の行へあふれた分だけ送る。行の長さは揃えない。上の行と下の行は、札の高さの約4分の1を重ねる。

## 5. API

| メソッド | パス | 意味 |
|---|---|---|
| GET | `/api/state` | 公開情報。山札の中身と除外札は含まない |
| POST | `/api/start` | `{players, humans, seed, sequence, item_set}` で開始 |
| POST | `/api/action` | `{kind: take\|collect\|abandon\|pass, card_id?, card_ids?}` |
| POST | `/api/reset` | スタート画面に戻す |

CPU は約 0.7 秒ごとに1手進む。
