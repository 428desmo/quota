# Quota Web 実装仕様

| 項目 | 内容 |
|---|---|
| 版 | 1.1 |
| 対応する規則 | quota_rule_v1.3.md |
| 対象 | `python -m quota.web` |

規則の正本はルール文書である。本書はブラウザ版の画面と通信だけを定める。盤面はサーバが1つ持ち、開いているブラウザは同じゲームを見る。

## 1. 起動

```bash
python -m quota.web
```

`0.0.0.0:8000` で待ち受ける。同じネットワークの端末は、起動したマシンのアドレスに `:8000` を付けて開く。

## 2. 画面

1. スタート画面。人数（3か4）、人間の席の数、任意のシード、上級ルールの有無を選ぶ。「スタート」でゲームが始まる。
2. ゲーム画面。市場と各席の注文、買い付け、出荷記録を出す。手番の席が人間なら、そのブラウザから行動できる。CPU の手番はサーバが進める。
3. 終了時は結果を重ねて出す。「OK」でスタート画面に戻る。
4. 「途中でやめて最初からやり直す」は、ゲーム中いつでもスタート画面に戻す。全員の画面が戻る。

見た目は枠線と絵文字、文字だけとする。カード id は出さない。数字は A・J・Q・K ではなく 1・11・12・13 と書く。銀貨に数字はない。

注文の札と買い付けた札は同じ列に置く。買い付けた札どうしは幅の約3分の1を重ね、注文の札と買い付けの左端は幅の約4分の1を重ねる。数字は札の左上に置き、重なっても読めるようにする。出荷記録も、隣り合う札が幅の約3分の1重なる。

| 商品 | 絵文字 |
|---|---|
| 香辛料 | 🌶️ |
| 絹 | 🎀 |
| 茶 | 🍵 |
| 宝石 | 💎 |
| 銀貨 | 🪙 |

「入港後まだ取引なし」は、膠着で場を入れ替えたあと、まだ誰も取っていない状態である。

## 3. 操作

- 注文がないとき、市場の通常カードを押すとその注文を請け負う。銀貨は押せない。パスはボタン。
- 注文があるとき、取れるカードを押した順に選ぶ。もう一度押すと選択を外す。「買い付ける」でその順に取る。順は出荷記録の順になる。
- 「注文を取り消す」と「パス」はボタン。

## 4. アニメーション

カードを取ったとき、市場に出ているその札そのものを、置かれる席まで動かす。別の札を市場の上に作って飛ばすことはしない。納品や補充の細かい軌道は持たない。

## 5. API

| メソッド | パス | 意味 |
|---|---|---|
| GET | `/api/state` | 公開情報。山札の中身と除外札は含まない |
| POST | `/api/start` | `{players, humans, seed, sequence}` で開始 |
| POST | `/api/action` | `{kind: take\|collect\|abandon\|pass, card_id?, card_ids?}` |
| POST | `/api/reset` | スタート画面に戻す |

CPU は約 0.7 秒ごとに1手進む。
