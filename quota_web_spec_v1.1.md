# Quota Web 実装仕様

| 項目 | 内容 |
|---|---|
| 版 | 1.2 |
| 対応する規則 | quota_rule_v1.11.md |
| 対象 | `python -m quota.web` |

規則の正本はルール文書である。本書はブラウザ版の画面と通信だけを定める。盤面はサーバが1つ持ち、開いているブラウザは同じゲームを見る。

## 1. 起動

```bash
python -m quota.web
```

`0.0.0.0:8000` で待ち受ける。同じネットワークの端末は、起動したマシンのアドレスに `:8000` を付けて開く。

## 2. 画面

1. スタート画面。タイトルは太字で「QUOTA 揃えて、達成。」。「ク ォ ー タ」は「QUOTA」の幅に対して中央に置く。「ノルマは、自分で決めろ。」は、タイトルとサブタイトルを合わせた幅に対して中央に置く。ルール説明の OK は中央に置く。「基本ルール」「上級モード」「ヒント」は、付録Dの対応する節を重ねて出し、「OK」で閉じる。文書で太字の語は画面でも太字にする。プレイ中の見出しは「QUOTA 揃えて、達成。」とする。ロビーで卓を新設するか、既存の卓に参加する。新設時に決めるのは人数（3か4）、アイテムセット、自分の名前、任意のシード、OKタイムアウト（秒、既定 3）、手番タイムアウト（秒、既定 30）、並び順ボーナス、称号ボーナス、左利きモード。人間の人数は決めない。シードとOKタイムアウトは小さい入力枠で同じ行に置く。並び順ボーナスと称号ボーナスは同じ行に置く。「卓を新設」は中央に置く。名前は自分の席にだけ付き、空欄は「あなた」になる。開始時に空いている席はCPUになる。定員に達している卓、または対局が始まっている卓には観戦で入る。操作のない状態が3分続くと卓は消える。選択肢は `quota_item_sets_v1.0.json`。既定は交易品。直前の対局の選択肢（シードと名前を除く）は、開始時から状態取得の応答に `quota_options` クッキーで載せる。自分の名前は、そのブラウザの `quota_name` クッキーに残す。参加していたブラウザは、次に自分が主催してもその内容が初期値になる。「スタート」でゲームが始まる。
2. ゲーム画面。場札と各席のノルマ、収集札、実績を出す。観戦者は「オブザーバー某が観戦しています」と表示する。手番の操作は、その席の参加者だけができる。自分以外に人間がいるとき、手番タイムアウトまで操作がないと、CPUが1手代行し、全員に約1秒その旨を出す。自分以外に人間がいないとき（対局中に明示的に抜けて1人になった場合を含む）は手番タイムアウトは起きず、出番時間は無制限になる。一時的な代行ではその人は人間のままなので、他に人間がいれば制限時間は続く。明示的に抜けると待たずにCPUが代行する。リーダーが抜けると、残っている人のうち参加が早かった人がリーダーになる。用語は規則の用語を使う。カードの名前・絵文字・数字ラベル・色は、そのゲームのアイテムセットに従う。手番の席が人間なら、そのブラウザから行動できる。CPU の手番はサーバが進める。各席のノルマの行は最初から1行分を空けておく。実績は1段のあいだその高さだけにし、札は見出しのすぐ横に置く。段が増えたら、その段数ぶん席の高さを広げ、下の段が上の段より手前に重なる。
3. 終了時は、印の換算が終わってから結果を重ねて出す。「次のゲームを始める」で、同じ参加者のままその卓を募集中に戻す。
4. 「ゲームから抜ける」で、その人だけが卓を離れてロビーに戻る。対局そのものは止めない。

席どうしは枠で区切らない。背景色の違いで席の境界を示し、手番の席だけ背景を変える。スマホの縦画面（幅 700px 以下）では、札の幅と高さをそれぞれ通常の 60% にする。ノルマと実績の見出しは左に縦書きにする。場札の見出しは横書きで、札の上の左に置く。「考えています」「場札からノルマ札を選びましょう。」「ノルマ達成まであとn枚。」は、その見出しの右端に、見出しより小さい文字で右寄せにする。場札は1段に並べる。「放棄」「パス」「次へ」は場札の枠の下辺に重ね、下にはみ出してよい。右寄せにする。盤面の高さを変えない。スタート画面の左利きモードでは左に重ねる。連続パスが人数に達したときの入れ替え確認も、盤面の上に重ねる。

見た目は枠線と絵文字、文字だけとする。カード id は出さない。数字欄はアイテムセットの `rank_labels`、ワイルドは `wild_rank_label`（交易品では 1〜13 と `＊`）を書く。縁は実線のまま、数字の色だけセットの色を使う。

達成ボーナスが付く束が実績に着いたとき、「ひと組10枚以上の達成ボーナス🟢🟢🟢」のように、届いた段（7枚以上は🟢、10枚以上は🟢3つ、13枚は🟢6つ）を、達成した席の中央に小さめの文字で1秒出す。実績では、達成ボーナスを小さい 🟢 で置く。7枚以上の束は7枚目に1つ、10枚以上はさらに10枚目に2つ、13枚はさらに13枚目に3つ。13枚の +6 は、この +1、+2、+3 の合計である。上級ルールのとき、隣り合う札の並び順ボーナスは、つながりの後ろの札に、その組の点数だけ小さい 🟣 を縦に並べる。印は数字の下で、数字とも印どうしとも重ならない。

ノルマ札と収集札は同じ列に置く。収集札どうしは幅の約3分の1を重ね、ノルマ札と収集札の左端は幅の約4分の1を重ねる。数字は札の左上に置き、重なっても読めるようにする。実績は、隣り合う札が幅の約7割重なる。数字は左端に寄せる。

終了時の得点の枠は、動いている札より手前に出す。

交易品セットの対応は、香辛料 🌶️、絹 🎀、茶 🫖、宝石 💎、銀貨 🪙。他のセットは JSON の絵文字と名前を使う。

状態行は「膠着状態 0」または「膠着状態 1」と書く。1 は、膠着で場を入れ替えたあと、まだ誰も取っていないときである。プレイ中の状態行に「上級ルール」とは書かず「上級」とし、アイテムセット名は出さない。

## 3. 操作

- ノルマがないとき、場札の通常カードを押すとノルマ札にする。銀貨は押せない。パスはボタン。メッセージは「場札からノルマ札を選びましょう。」
- ノルマがあるときのメッセージは「ノルマ達成まであとn枚。」
- プレイ中の得点はノルマの数字だけを足す。達成ボーナスと並び順ボーナスの印は、実績の札の上に置いたままにし、数字には足さない。印は通常の絵文字より小さくする。
- ゲーム終了後、先頭の席から順に、印を実績の札から得点へ素早く動かす。印が得点に着くたびにその印を消し、表示中の得点を 1 点足す。称号ボーナスがある席は、そのあと「CPU2が『生粋の買い付け』を達成したので+5のボーナス獲得」のように重ねて讃え、その点数を1点ずつ足す。全部終わってから順位を出す。ボタンは「次のゲームを始める」。
- 山札切れのときは、途中でやめるボタンを消したあと「山札がなくなりました。得点計算に映ります」と OK を出す。人間の席は、その席を操作した端末（開始した端末は、まだ誰も操作していない人間の席）が OK を押す。全員が押すか、OKタイムアウト（既定 3 秒）が過ぎるまで印の移動に入らない。CPU の席は表示から 0.5 秒で OK した扱いにする。席を持たない端末は OK を押せないが、ボタンは出し、誰の OK を待っているかを書く。順位の見出しは「ゲーム終了」とする。
- 席の得点の横に、並び順の累積、達成回数、残り枚数は出さない。
- 連続パスは「0/3」のように、分子が連続回数、分母が人数である。人数に達して場札を入れ替えるときは、入れ替える前に「全員がパスをしたので、場札をリフレッシュします」と OK を出す。OK の押し方、CPU の 0.5 秒、オブザーバー、OKタイムアウトは山札切れの確認と同じ。
- 実績の札は場札より一回り小さくし、着いたときに縮む。段の数だけ席を広げる。
- ノルマがあるとき、取れるカードを1枚押すとその場で集める。手番は続く。押せないカードは薄くする。「集める」ボタンはない。続けて別の有効札も取れる。それ以上取らないときは、この手番で場札から1枚も取っていなければ「パス」、1枚以上取っていれば「次へ」で次の席に回す。1枚以上集めたうえで場に取れる札が残っていなければ、ボタンなしで次の席に回る。ノルマが揃ったときは、束が実績へ移り終わるまで次の席の処理を始めない。
- 「放棄」と「パス」はボタン。

## 4. アニメーション

場札から札を取ったあとも、その手番のアニメーションが終わるまでは取った位置を空白のまま残す。アニメーションが終わり、次の席が操作できる直前に、残った札を左へ寄せ、空いた右側へ山札から補充する。

カードを取ったとき、場札に出ているその札そのものを、置かれる位置まで動かす。移動が終わるまで、ノルマ・収集・実績の側には同じ札を出さない。着いてから、そこが収集札または実績になる。別の札を場札の上に作って飛ばすことはしない。

1 のノルマ札は、場札から一度収集の列に置き、約 0.1 秒たってから実績へ移す。集めてノルマが揃ったときも、揃った束をいったん収集の列に見せ、一呼吸（約 0.4 秒）置いてから実績へ移す。実績へ直接は置かない。

実績は、1行に収まらない長さになったら次の行へあふれた分だけ送る。行の長さは揃えない。隣り合うミニカードは幅の約 7 割を重ね、数字は札の左端に寄せる。上の行と下の行は、札の高さの約4分の1を重ねる。

## 5. API

| メソッド | パス | 意味 |
|---|---|---|
| GET | `/api/state` | 公開情報。山札の中身と除外札は含まない |
| POST | `/api/table` | 卓を新設してリーダーとして入る |
| POST | `/api/join` | `{table, name}` で既存の卓に入る |
| POST | `/api/start` | リーダーが募集中の卓を開始する |
| POST | `/api/leave` | 卓から抜ける |
| POST | `/api/again` | 終了した卓を、残っている参加者のまま募集中に戻す |
| POST | `/api/action` | `{kind: take\|collect\|abandon\|pass, card_id?, card_ids?}` |
| POST | `/api/ack` | 確認の OK |

CPU は約 0.7 秒ごとに1手進む。
