# Quota CLI 実装仕様

| 項目 | 内容 |
|---|---|
| 版 | 1.1 |
| 対応する規則 | quota_rule_v1.3.md |
| 対象 | `python -m quota.cli` |

規則の正本はルール文書である。本書はターミナル版の操作、表示、CPU の手だけを定める。

## 1. 起動

```bash
python -m quota.cli --players 3 --humans 1
python -m quota.cli --players 4 --humans 2 --seed 7
python -m quota.cli --auto --seed 1
python -m quota.cli --sequence --auto --seed 1
```

| 引数 | 既定 | 意味 |
|---|---|---|
| `--players` | 3 | 席の数。2〜6 |
| `--humans` | 1 | 先頭からこの人数を人間にする。残りは CPU |
| `--seed` | なし | シャッフルと先手の乱数シード |
| `--auto` | オフ | 全員を CPU にする |
| `--sequence` | オフ | 上級ルール（並び順ボーナス）を適用する |

人間が1人のときの名前は「あなた」。2人以上なら「あなた1」から。CPU は「CPU1」から。

## 2. 表示

カードは `♥4 絹 #14` の形で出す。トランプ表記、商品名、カード id の順。`#14` は 0〜107 の通し番号で、同じマーク・数字が2枚あるときの識別用である。得点ではない。

| マーク | 商品 |
|---|---|
| ♠ | 香辛料 |
| ♥ | 絹 |
| ♣ | 茶 |
| ♦ | 宝石 |
| Joker | 銀貨 |

各手番の前に、手番番号、山札の枚数、連続パス、入港済みかどうか、市場、各席の得点・注文・買い付けを出す。山札の中身と除外札は出さない。

「入港済み」は枚数ではない。膠着で場を入れ替えたあと、まだ誰もカードを取っていないときだけ「あり」になる。

`--sequence` のときは、得点に積み付け（並び順ボーナス）を含め、各席にその内訳も出す。納品の最高点には積み付けを含めない。

## 3. 行動の文言

| 規則上の行動 | CLI の文言 |
|---|---|
| ノルマ札を取る | 注文を請け負う |
| カードを集める | 買い付ける |
| ノルマを放棄する | 注文を取り消す |
| パス | パス |

「注文する」は使わない。この言い換えは規則を変えない。

## 4. 人間の入力

注文がないとき、合法手を番号で並べる。`番号>` に1つの番号を入れる。

注文があるとき、買い付けられるカードを番号で並べ、取り消しは `a`、パスは `p` とする。買い付けるときは番号を空白区切りで入れる。1枚以上、残り必要数まで。同じ番号の重複は受け付けない。番号を書いた順が、出荷記録に置く順になる。

## 5. CPU

合法手から1つ選ぶ。

- 注文がないとき、A があれば A を請け負う。なければ数字が6に近いカードを請け負う。11以上しか大きい候補がないときは A を優先する。
- 注文があるとき、同じ商品を優先し、足りなければ銀貨を足して、残り必要数まで一度に買い付ける。
- 上級ルールのとき、直前のカードと数字がそろう順を優先して置く。銀貨は連鎖を切るので後回しにする。
- 残りが6枚以上で、場に同じ商品がなく銀貨だけ、または何もないときは、注文を取り消す。
- それ以外で取れなければパスする。

## 6. 終了表示

終了理由は「山札切れ」または「膠着の連続」。順位は規則 13.2 のとおりで、同点の席は同じ順位で並べる。上級ルールの合計点は積み付けを含む。
