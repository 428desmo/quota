# Quota CLI 実装仕様

| 項目 | 内容 |
|---|---|
| 版 | 1.2 |
| 対応する規則 | quota_rule_v1.12.md |
| 対象 | `python -m quota.cli` |

規則の正本はルール文書である。本書はターミナル版の操作、表示、CPU の手だけを定める。

## 1. 起動

```bash
python -m quota.cli --players 3 --humans 1
python -m quota.cli --players 4 --humans 2 --seed 7
python -m quota.cli --auto --seed 1
python -m quota.cli --sequence --auto --seed 1
```

| 引数 | 既定 | 意味 |
|---|---|---|
| `--players` | 3 | 席の数。2〜6 |
| `--humans` | 1 | 先頭からこの人数を人間にする。残りは CPU |
| `--seed` | なし | シャッフルと先手の乱数シード |
| `--auto` | オフ | 全員を CPU にする |
| `--sequence` | オフ | 上級ルール（並び順ボーナス、15.1節）を適用する |
| `--title` | オフ | 上級ルール（称号ボーナス、15.2節）を適用する |
| `--special` | オフ | 上級ルール（特殊アクション、15.3節）を適用する |
| `--item-set` | `trade` | アイテムセットの id または名前。一覧は `--list-item-sets` |
| `--list-item-sets` | オフ | アイテムセット一覧を出して終わる |

人間が1人のときの名前は「あなた」。2人以上なら「あなた1」から。CPU は「CPU1」から。

## 2. 表示

カードは `🎀4 絹 #14` の形で出す。絵文字、数字ラベル、アイテム名、カード id の順。数字ラベルと名前は、選んだアイテムセット（`quota_item_sets_v1.0.json`）に従う。既定は交易品で、数字は 1〜13、ワイルドは `＊`。`#14` は 0〜107 の通し番号で、同じ種類・数字が2枚あるときの識別用である。得点ではない。

各手番の前に、手番番号、山札の枚数、連続パス、膠着状態、場札、各席の得点・ノルマ・収集を出す。山札の中身と除外札は出さない。カードの商品名は出す。

「膠着状態」は 0 か 1 である。1 は、膠着で場を入れ替えたあと、まだ誰もカードを取っていないときである。

`--sequence` のときは、得点に並び順ボーナスを含め、各席にその内訳も出す。`--title` のときは、称号ボーナス（単色達成＋15、生粋の買い付け＋5。いずれも達成3回以上）を終了時の得点に含める。1回の達成の最高点には、並び順ボーナスも称号ボーナスも含めない。`--special` のときは、手番の最初に `d` でダブル、`r` で配り直しを宣言できる。空エンターなら通常の行動に進む。ダブルを宣言した直後、札を選ぶ前なら `c` で取り消せる。

## 3. 行動の文言

| 規則上の行動 | CLI の文言 |
|---|---|
| ノルマ札を取る | ノルマ札にする |
| カードを集める | 集める |
| ノルマを放棄する | 放棄 |
| パス | パス |

画面の用語は規則の用語を使う。アイテム名は選んだセットの名前を使う。

## 4. 人間の入力

ノルマがないとき、合法手を番号で並べる。`番号>` に1つの番号を入れる。

ノルマがあるとき、集められるカードを番号で並べ、放棄は `a`、パスは `p` とする。集めるときは番号を空白区切りで入れる。1枚以上、残り必要数まで。同じ番号の重複は受け付けない。番号を書いた順が、実績の並び順になる。ノルマが揃うまでは手番が続く。続けて集められる。取り終えたら `p` で次の席に回す。

## 5. CPU

合法手から1つ選ぶ。

- ノルマがないとき、1 があればそれをノルマ札にする。なければ数字が6に近いカードを選ぶ。11以上しか大きい候補がないときは 1 を優先する。
- ノルマがあるとき、同じ種類を優先し、足りなければワイルドを足して、残り必要数まで一度に集める。
- 上級ルールのとき、直前のカードと数字がそろう順を優先して置く。ワイルドは連鎖を切るので後回しにする。
- 残りが6枚以上で、場に同じ種類がなくワイルドだけ、または何もないときは、放棄する。
- それ以外で取れなければパスする。

## 6. 終了表示

終了理由は「山札切れ」または「膠着の連続」。順位は規則 13.2 のとおりで、同点の席は同じ順位で並べる。上級ルールの合計点は並び順ボーナスを含む。
